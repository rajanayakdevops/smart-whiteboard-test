import { useNavigate } from "react-router-dom";

const CreateMeeting = () => {
  const navigate = useNavigate();

  return <div>
    <h1> this is create meeting room </h1>
  </div>
};

export default CreateMeeting;
